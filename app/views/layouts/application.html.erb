<!DOCTYPE html>
<html>
<head>
  <title>Task</title>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-default navbar-collapse-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <strong class="text-success"><%= link_to "USER EXPENDITURE", homes_path, :class=>"navbar-brand" %></strong>
      </div>
      <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
          <li><%= link_to 'HOME', homes_path %></li>
          <li><%= link_to 'Expense', expenses_path %></li>
          <li><%= link_to 'Income', incomes_path %></li>
         
          <% if user_signed_in? %>
          <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
          <% else %>
          <li>
            <%= link_to('SIGN IN', new_user_session_path)%>
          </li>
          <% end %>
           <li><%= link_to "SIGN UP", new_user_registration_path %></li>
         </ul>
    </div>
  </div>
</nav>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <div class = "container" style = "padding-top:20px;">
<%= yield %>

</body>
</html>
